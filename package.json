{
  "name": "wcplayer",
  "version": "0.0.4",
  "private": true,
  "packageManager": "pnpm@7.6.0",
  "description": "",
  "author": "Chengbo Xie<https://github.com/iChengbo>",
  "license": "MIT",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "postinstall": "husky install",
    "build": "pnpm -r --filter=./packages/* run build",
    "clean": "pnpm -r --filter=./packages/* run clean",
    "test": "pnpm -r --filter=@wcplayer/components-vue run test"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.0.3",
    "@commitlint/config-conventional": "^17.0.3",
    "@types/jest": "^28.1.6",
    "@types/node": "^18.6.4",
    "husky": "^8.0.1",
    "jest": "^28.1.3",
    "jest-environment-jsdom": "^28.1.3",
    "rimraf": "^3.0.2",
    "ts-jest": "^28.0.7",
    "typescript": "^4.7.4"
  }
}
